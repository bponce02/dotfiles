# colors.json is regenerated per-machine from the wallpaper (pywal), so keep it untracked.
# settings.json IS tracked: it holds the bar layout, wallpaper prefs, keybinds, etc.
config/.config/noctalia/colors.json
config/.config/noctalia/plugins/*/settings.json
